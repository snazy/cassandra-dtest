RULE Kill node during repair validation
CLASS org.apache.cassandra.db.compaction.CompactionManager
METHOD doValidationCompaction
AT ENTRY
# set flag to only run this rule once.
IF NOT flagged("done")
DO
   flag("done");
   System.exit(1);
ENDRULE

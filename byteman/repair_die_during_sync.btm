RULE Kill node during streaming
CLASS org.apache.cassandra.streaming.StreamSession
METHOD messageReceived
AT ENTRY
# set flag to only run this rule once.
IF NOT flagged("done")
DO
   flag("done");
   System.exit(1);
ENDRULE
